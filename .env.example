# Copy to .env and fill in values
# Local dev (pickle fallback — no Postgres needed):
ANTHROPIC_API_KEY=sk-ant-...
API_KEY=dev-key

# Production (pgvector):
# DATABASE_URL=postgresql://rag:rag@localhost:5432/rag
# API_KEY=your-secret-key-here
